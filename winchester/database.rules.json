{
  "rules": {
    "winchester": {
      ".read": true,
      ".write": true,
      "members": {
        ".validate": "newData.hasChildren()"
      },
      "log": {
        ".validate": "newData.hasChildren()"
      },
      "event": {
        ".validate": "newData.hasChildren(['name', 'date'])"
      },
      "checkedIn": {
        ".validate": "newData.hasChildren()"
      },
      "drinks": {
        ".validate": "newData.hasChildren()"
      },
      "$other": {
        ".validate": false
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
